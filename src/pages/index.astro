---
import { Font } from "astro:assets";

import Links from "../components/Links.astro";
import Authors from "../components/Authors.astro";
import Notes from "../components/Notes.astro";
import type { Author, Note, Link } from "../types/types";

import Table from "../components/Table.astro";

import { Icon } from "astro-icon/components";

import "../styles/global.css";
import "katex/dist/katex-swap.css";

import { Content, frontmatter } from "../paper.mdx";

interface Frontmatter {
  title: string;
  authors: Author[];
  conference?: string;
  notes?: Note[];
  links?: Link[];
  description?: string;
  favicon: string;
  thumbnail?: string;
  theme?: "device" | "light" | "dark";
}

const {
  title,
  authors,
  conference,
  notes,
  links,
  description,
  favicon,
  thumbnail,
  theme = "device",
} = frontmatter as Frontmatter;

const baseUrl = import.meta.env.BASE_URL;
const prefix = baseUrl.endsWith("/") ? baseUrl : baseUrl + "/";

const url = new URL(Astro.url.pathname, Astro.site).href;
const imgUrl = thumbnail
  ? new URL(thumbnail, Astro.site).href
  : null;
---

<!doctype html>
<html lang="en" data-theme={theme}>
  <head>
    <meta charset="UTF-8" />
    {description && <meta name="description" content={description} />}
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta content="somsubhra" name="author">
    <meta content="AI4Fashion,AI,FLORA,NeRA,adapters,PEFT,LoRA,Gen AI,Kolmogorov Arnold Networks,fashion dataset,WACV,WACV 2026,Applications,Candle Lab IITR" name="keywords">

    <link rel="icon" type="image/svg+xml" href={prefix + favicon} />

    <meta property="og:title" content={title} />
    {description && <meta property="og:description" content={description} />}
    <meta property="og:type" content="website" />
    <!--{thumbnail && <meta property="og:image" content={prefix + thumbnail} />}-->
    <meta property="og:url" content={url} />
    {imgUrl && <meta property="og:image" content={imgUrl} />}
    <!-- Twitter -->
<meta name="twitter:card" content={imgUrl ? "summary_large_image" : "summary"} />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
{imgUrl && <meta name="twitter:image" content={imgUrl} />}

    <Font cssVariable="--font-noto-sans" preload />

    <title>{title}</title>
  <script src="/WACV2026-FLORA-Dataset-NeRA-Adapter/js/global.js" type="module"></script>
  </head>
  <!--<body
    class="overflow-x-hidden mx-auto max-w-[50rem] px-6 prose prose-lg prose-zinc dark:prose-invert prose-a:text-blue-500 prose-a:dark:text-blue-300 prose-a:no-underline prose-a:hover:underline prose-a:font-normal prose-code:bg-zinc-200 prose-code:dark:bg-zinc-800 prose-code:text-zinc-800 prose-code:dark:text-zinc-200 prose-code:px-1.5 prose-code:py-0.5 prose-code:rounded prose-code:font-medium prose-code:before:content-none prose-code:after:content-none mx-auto max-w-[50rem] bg-white px-6 pt-16 pb-6 font-(family-name:--font-noto-sans) dark:bg-zinc-900"
  >-->
  
  <body class="overflow-x-hidden mx-auto max-w-full bg-white dark:bg-zinc-900 pt-0 pb-0 font-(family-name:--font-noto-sans)
              prose prose-lg prose-zinc dark:prose-invert 
              prose-a:text-blue-500 prose-a:dark:text-blue-300 
              prose-a:no-underline prose-a:hover:underline prose-a:font-normal 
              prose-code:bg-zinc-200 prose-code:dark:bg-zinc-800 
              prose-code:text-zinc-800 prose-code:dark:text-zinc-200 
              prose-code:px-1.5 prose-code:py-0.5 prose-code:rounded 
              prose-code:font-medium prose-code:before:content-none 
              prose-code:after:content-none">

  <!--
    <div>
      <h1 class="text-center text-3xl font-bold text-[#3b3f8f]">{title}</h1>
      <div class="flex flex-col items-center gap-6">
        <Authors authors={authors} />
        {conference && <div><font color="green">{conference}</font></div>}
        {notes && <Notes notes={notes} />}
        {links && <Links links={links} />}
      </div>
    </div>-->

<header id="pageHeader" class="w-full sticky top-0 z-50 flex items-center justify-between py-2 px-4 transition-colors duration-300">
  <div class="relative flex-shrink-0">
    <button 
      id="menuButton" 
      class="px-3 py-1 rounded-lg text-slate-800 hover:bg-slate-100 transition-all duration-200 flex items-center gap-1"
      aria-expanded="false"
    >
      <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
      </svg>
      Menu
    </button>

    <div id="menuDropdown" class="absolute left-0 mt-2 w-40 bg-white rounded-lg shadow-lg opacity-0 invisible transition-all duration-200">
      <a href="/WACV2026-FLORA-Dataset-NeRA-Adapter" class="flex items-center gap-2 px-4 py-2 text-slate-800 hover:bg-blue-100 rounded-t-lg"><Icon name="ri:home-fill" /> Home</a>
      <a href="mailto:sparsh.mittal@ece.iitr.ac.in" target="_blank" class="flex items-center gap-2 px-4 py-2 text-slate-800 hover:bg-blue-100 rounded-t-lg"><Icon name="ri:mail-fill" /> Contact</a>
    </div>
  </div>

<h1 class="text-center text-base font-medium text-black flex-1 mx-4 truncate">
Welcome to our Project Page!
  </h1>

  <div class="flex-shrink-0">
    <button 
      id="shareButton" 
      class="px-3 py-1 rounded-lg text-slate-800 hover:bg-slate-100 transition-all duration-200 flex items-center gap-1"
    >
      <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" d="M4 12v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8M16 6l-4-4-4 4M12 2v14" />
      </svg>
      Share
    </button>
  </div>
</header>

<h1 class="mx-auto max-w-[50rem] px-6 text-center text-3xl font-bold text-[#3b3f8f]">{title}</h1>
<div class="flex flex-col items-center gap-6 px-4 mt-4">
  <Authors authors={authors} />
  {conference && <div class="text-green-600">{conference}</div>}
  {notes && <Notes notes={notes} />}
  {links && <Links links={links} />}
</div>


    <main class="mt-6 mx-auto max-w-[50rem] px-6">
      <Content components={{ table: Table }} />
    </main>
<footer class="w-full bg-[#cdc5ed] mt-12">
  <div class="flex w-full flex-col items-center justify-between gap-3 px-4 py-4 text-sm text-slate-700 md:flex-row">
  <p class="text-center md:text-left">
      <a
        href="https://github.com/CandleLabAI/WACV2026-FLORA-Dataset-NeRA-Adapter/tree/gh-pages" 
        target="_blank"
        class="!text-violet-800 !underline hover:!text-violet-900"
      >
        Website Code</a>, adapted from 
      <a
        href="https://github.com/RomanHauksson/academic-project-astro-template" 
        target="_blank"
        class="!text-violet-800 !underline hover:!text-violet-900"
      >
        Roman Hauksson's Academic Astro template</a>
      licensed under
      <a 
        href="http://creativecommons.org/licenses/by-sa/4.0/" 
        target="_blank"
        class="!text-violet-800 !underline hover:!text-violet-900"
      >
        CC BY-SA 4.0</a>.<br>
        Open-sourced with <Icon name="ri:heart-fill" class="text-red-600 inline-block" /> for the community.
    </p>
    <div class="text-center text-xs text-slate-600 md:text-right">
      Page Visits: <a href="https://www.freecounterstat.com" target="_blank" title="free website counter"><img src="https://counter1.optistats.ovh/private/freecounterstat.php?c=ad8r987d64gblgbae5gxbwmjaryjwzs1" border="0" title="free website counter" alt="free website counter"></a>
    </div>

  </div>
</footer>
  <button id="goTop" class="go-to-top"><Icon name="ri:arrow-up-line" class="w-5 h-5" /></button>
  </body>
</html>
